# RSN-Recursive-Spatial-Network
RSN (Recursive Spatial Network) is a prediction framework based on recursive expansion over a 6-dimensional state space (Hypercube). The model generates future state trajectories through recursive node expansion and spatial hashing.
